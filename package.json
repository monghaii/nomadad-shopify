{
  "name": "nomadad-shopify",
  "version": "1.0.0",
  "private": true,
  "description": "Nomadad Shopify theme",
  "scripts": {
    "deploy": "set -a && . ./.env && set +a && shopify theme dev",
    "dev": "set -a && . ./.env && set +a && shopify theme dev",
    "check": "set -a && . ./.env && set +a && shopify theme check",
    "pull": "set -a && . ./.env && set +a && shopify theme pull",
    "push": "set -a && . ./.env && set +a && shopify theme push"
  }
}
